<div class="jumbotron">
    <div class="container">
        <h1 class="text-center">Burger Chomper</h1>
        <img src="../../../public/assets/img/burger.png">
        </p>
    </div>
</div>


<div class="row">
    <div class="col-xs-6 col-sm-6 col-md-6 col-lg-6">
        <ul class="devoured">
            {{#each burger}}
              {{#unless devoured}}
              {{/unless}}
            {{/each}}
        </ul>
        <input id="eatBurger" type="text">
        <button type="button" class="btn btn-large btn-block btn-success">Eat a favorite</button>
    </div>
</div>

<div class="row">
    <div class="col-xs-6 col-sm-6 col-md-6 col-lg-6">
        <ul class="served">
             {{#each burger}}
              {{#unless served}}
              {{/unless}} 
            {{/each}}
        </ul>
        <input id="addBurger" type="text">
        <button type="button" class="btn btn-large btn-block btn-success">Add a favorite</button>
    </div>
</div>



